const btnAdd = document.getElementById('submit-book');
const txtTitle = document.getElementById('title');
const txtAuthor = document.getElementById('author');

export { btnAdd, txtTitle, txtAuthor };
